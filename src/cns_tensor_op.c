#include <assert.h>

#include "cns_tensor_op.h"

/* cns_tensor *cns_tensor_slice(const cns_tensor *src, cns_tensor *dst, */
/* 			uint32_t dim, uint32_t start, uint32_t len) */
/* { */
/* 	assert(cns_tensor_is_valid(src) && cns_tensor_is_valid(dst)); */
/* 	assert(dst->ndim == src->ndim); */
/* 	uint32_t i; */
/* 	for (i = 0; i < dst->ndim; i++) */
/* 		assert(i == dim ? dst->dims[i] == len : dst->dims[i] == src->dims[i]); */


/* } */
